somewhere, transforms identifiers into declarations, before analyser

//must get rid of stack after fors and stuff.

generics can only take in classname, ?, extends.

//functions neeed to check for super classes/parent classesssss

//remember that something like Object.toString().length() dont work rn

//it wants something like ArrayList.add(TT;) not java.lang.Object

you are workin on add func;

upload to github

parser, dec sending decide? idk